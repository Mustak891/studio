rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // WARNING: TEMPORARILY OPEN FOR DEBUGGING
      // Allows anyone to read and write to any user's document.
      // This is NOT secure for production.
      allow read: if true;
      allow write: if true; 
    }
  }
}
